/// The release version embedded in SwiftDebt runtime output and reports.
package enum SwiftDebtRelease {
    // swiftdebt-release-version:start
    package static let version = "0.8.0"
    // swiftdebt-release-version:end
}
